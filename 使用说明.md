# StudyMKD 使用说明

StudyMKD 是一个本地优先的视频学习笔记工具。它可以从课程目录截图中识别章节，为每个章节保存笔记、重点、截图、复习卡片和学习资料，并将课程目录、笔记内容与视频截图导出为单个 Markdown 文件。

## 1. 运行环境

### Windows 安装版

- Windows 10 或 Windows 11
- 建议至少预留 1 GB 可用磁盘空间；实际占用取决于截图和托管文件数量
- 安装桌面版后不需要 Node.js、npm 或单独安装浏览器

### 从源码运行

- Node.js 20 LTS 或更高版本
- npm 10 或更高版本
- 支持现代 Web API 的 Chromium 浏览器，例如 Chrome、Edge 或 Codex 内置浏览器
- 建议至少预留 1 GB 可用磁盘空间；实际占用取决于截图和托管文件数量

Node.js 安装完成后，可以在 PowerShell 中检查版本：

```powershell
node --version
npm --version
```

### 可选的 C++ 后端环境

当前前端已经可以独立使用，普通用户不需要编译 C++ 后端。需要开发原生后端时再安装：

- Visual Studio 2022 Build Tools，包含“使用 C++ 的桌面开发”
- CMake 3.24 或更高版本
- vcpkg
- C++20 编译器

C++ 依赖由 `backend/vcpkg.json` 声明，包括 Drogon、OpenCV、Tesseract 和 SQLite。

## 2. 首次安装

桌面版直接运行：

```text
frontend/release/StudyMKD-0.1.0-Setup.exe
```

当前安装程序没有购买商业代码签名证书，Windows SmartScreen 可能显示“未知发布者”。请确认安装包来自本项目后再运行。

从源码运行时，在 PowerShell 中进入项目的前端目录：

```powershell
cd frontend
npm install
```

`npm install` 会安装依赖，并将本地 OCR 所需的中英文模型复制到前端资源目录。课程目录识别不需要调用在线 OCR 服务。

## 3. 启动项目

安装版从桌面或开始菜单启动 `StudyMKD`。

桌面开发模式：

```powershell
cd frontend
npm run desktop:dev
```

浏览器开发模式：

在 `frontend` 目录运行：

```powershell
npm run dev
```

终端显示启动成功后，在浏览器打开：

```text
http://127.0.0.1:5173/
```

请保持运行命令的 PowerShell 窗口开启。按 `Ctrl+C` 可以停止服务。

桌面版通过 Electron IPC 提供本地文件夹能力；浏览器开发版通过 Vite 桥接提供。因此“一键打开文件夹”和“在资源管理器中定位文件”在桌面版或 `npm run dev` 下可用。

## 4. 基本使用流程

1. 点击左侧课程区域的新建按钮，填写课程名称和预计完成时间后创建课程。预计完成时间支持天、周、月三种单位，并显示在课程目录的进度区域。
2. 点击“导入目录”，上传包含视频课程目录的图片。
3. 等待本地 OCR 完成，在导入窗口中检查并修正章节名称与自动识别的层级。
4. 选择追加或替换：追加只加入新章节并保留已有笔记；替换会用新识别结果重建当前课程目录。
5. 点击左侧章节，在编辑区添加笔记、重点、代码块、视频截图和章节回顾。
6. 在章节信息中设置学习状态、内容标记、标签和复习卡片。
7. 完成后点击“导出 Markdown”，保存当前课程的单个 `.md` 文件。

课程章节较多时，可以使用左侧进度区域下方的“搜索章节”输入框按名称筛选目录。搜索框下方还可以选择全部、未开始、学习中或已完成状态，名称搜索与状态筛选可组合使用。结果会显示“匹配数 / 章节总数”，点击清除图标可清空名称搜索，切换课程会恢复“全部”状态。

如果章节带有标签，状态筛选下方会出现标签筛选条，点击标签即可只看带该标签的章节，再次点击或点击清除图标取消筛选。

点击顶栏放大镜图标可以打开全局搜索，跨所有课程匹配章节标题、笔记内容、标签、截图说明、章节回顾和复习卡片，点击任意结果会直接跳转到对应章节。

大纲底部的章节管理按钮支持：上移、下移调整章节顺序；复制当前章节（笔记、截图、卡片和附件一起复制，标题自动加“（副本）”）；把章节移动到其他课程（移动后原课程的托管路径记录会清除，导出时会按新课程目录重新生成）。

点击顶部课程名称可以打开课程管理菜单。每个课程右侧提供重命名和删除按钮。删除课程会删除应用中的章节、笔记、截图、附件记录和该课程的应用托管副本，但不会删除链接的硬盘源文件或手动导出的文件。删除最后一个课程后，应用会显示空课程页面，可重新新建课程。

新建或重命名课程时，程序会忽略名称首尾空格、连续空格、英文字母大小写和全角/半角差异进行查重。同名时不会直接创建或改名，可以选择：

- 使用已有课程：切换到已经存在的课程，不删除任何数据。
- 替换已有课程：经过二次确认后删除已有同名课程，再用新课程或当前课程替换它。

## 5. 专注笔记小窗

在桌面版顶部点击“专注笔记小窗”图标，会打开一个独立的小窗口。它只保留当前课程与章节、富文本笔记、视频时间、学习计时和前后章节切换，适合放在视频播放器旁边使用。

- 小窗默认始终置顶，点击图钉可取消或恢复置顶。
- 点击外链图标可回到主窗口。
- 主窗口和小窗通过 IndexedDB 与 Electron 消息实时同步。
- 小窗编辑当前章节时，主窗口对应编辑器会暂时只读；正常关闭后立即解除，异常关闭最多约 5 秒自动解除。
- 上一章、下一章按钮可切换笔记；底部可修改视频时间并开始或暂停计时。
- 浏览器版也能打开普通弹出窗口，但浏览器无法保证始终置顶。

桌面版与浏览器版数据不会自动共享。要迁移现有课程，请在旧版本顶部点击“备份”导出 JSON，然后在桌面版中选择“备份”并导入。

## 6. 学习倒计时

在右侧“学习信息”中为当前章节设置计划分钟数，然后点击“学习中”，倒计时会立即开始。开始时间和已累计用时会自动保存，刷新页面或切换到其他章节后仍按真实经过时间计算。

- 点击暂停图标可暂停，点击播放图标可继续。
- 点击重置图标可将当前章节的累计用时归零。
- 倒计时归零后会显示超时并继续累计，不会自动把章节标记为完成。
- 点击“已完成”会停止该章节计时，并将它计入课程进度。
- 暂停、完成或重置计时的同时会写入学习历史片段；顶栏“学习统计”可以查看今日、本周、本月和累计学习时长、最近 12 周的学习热力图，以及每个章节的计划分钟数与实际用时的对比。
- 重置章节计时会同时清除该章节的学习历史记录。
- 当所有章节均为“已完成”、课程进度达到 100% 时，左侧课程目录会显示全部章节的累计实际用时。
- 学习计时只保存在应用中，不加入精简版 Markdown 导出文件。

将章节从“学习中”切换到“未开始”会停止计时但保留已累计用时，之后可以再次点击“学习中”继续。

## 7. 学习计划与完成时间分析

点击顶部“学习计划”打开当前课程的规划器。设置开始日期、目标周期和每周可学习时段，再根据需要调整专注时长、休息时长、最短学习时间和缓冲比例。可添加多个星期与时间段，保存后会显示在当前课程中。

规划器会自动生成：

- 未完成章节的剩余学习时间
- 扣除休息后的每周有效学习时间
- 预计完成日期
- 与目标周期相比提前或延后的天数
- 最近 12 个具体学习安排

程序先根据每章的“计划分钟数”和历史速度修正计算预计总用时，再减去已经累计的实际学习时间，最后对这个“预计剩余学习时间”加入缓冲比例。例如预计还需 100 分钟，设置 15% 缓冲后会按 115 分钟排期。缓冲只影响完成日期和学习安排，不会修改章节计划时间或实际计时。已有完成章节时，程序会比较实际用时与原计划用时，在 `0.7x–2.0x` 范围内修正后续预测。章节完成、状态变化或倒计时继续增长后，预测会自动更新。

课程目录进度区域会同时显示目标周期和动态预测日期。旧课程也可以在学习计划中补充或修改目标周期。时间安排只保存在本地，不会上传。

## 8. 笔记与代码块

编辑器支持粗体、斜体、荧光重点、标题、列表、引用和代码块。插入代码块后，可以从工具栏选择 C++、Python、JavaScript、Rust 等语言；导出 Markdown 时会保留对应的代码围栏语言。

点击编辑器工具栏的搜索图标可以搜索当前章节的笔记内容。全部匹配会被高亮，当前匹配使用更深的颜色；点击上下箭头切换结果，按 `Enter` 跳到下一个，按 `Shift+Enter` 返回上一个，按 `Esc` 关闭搜索。

选中一段笔记后可以创建复习卡片。卡片的问题和答案均可编辑，也可以删除并重新创建。

点击顶栏的“今日复习”数字可以进入复习工作台：先看到今天到期的卡片清单，点击“开始复习”后逐张显示问题，点击“显示答案”查看答案，再按“忘记 / 困难 / 掌握”评级。评级会自动更新卡片的到期时间（忘记为今天再次复习、困难为 2 天后、掌握为 5 天后），复习结束后显示本次结果统计。复习工作台会汇总所有课程中到期的卡片。

## 9. 截图与批注

点击“添加截图”可导入视频画面，并为截图填写说明和视频时间点。双击截图或点击编辑图标可进入批注界面，支持：

- 画笔
- 箭头
- 文字
- 区域擦除
- 清空全部批注
- 颜色与粗细调整
- 撤销和重做

截图上传时会在 `data/managed/课程/章节/screenshots/` 创建应用托管副本。保存后的批注仍保存在浏览器中，导出 Markdown 前会把最新版本同步到该路径。

## 10. 章节文件与项目文件夹

导入文件、多个文件或项目文件夹时，可以选择两种方式：

### 项目托管目录

文件内容会复制到浏览器 IndexedDB。点击“打开文件/项目”时，应用会将当前章节文件同步到：

```text
项目根目录/data/managed/课程名称 [编号]/章节名称 [编号]/
```

优点是原文件移动后仍可预览和导出；缺点是会额外占用磁盘空间。删除附件时会删除应用记录以及 `data/managed` 中对应的托管副本，但不会删除最初导入的源文件，也不会删除你手动导出的文件。

### 链接原文件

应用只保存浏览器授予的文件句柄，不复制源文件。原文件被移动、重命名、删除或权限失效后，链接可能无法继续使用。删除章节中的链接只会移除应用记录，绝不会删除硬盘上的源文件。

链接模式依赖 File System Access API。如果浏览器不支持，应用会禁用该选项，此时请使用项目托管目录。

## 11. 数据保存与备份

课程、笔记、截图和复习卡片默认保存在当前浏览器配置的 IndexedDB 中，不会自动上传到网络。清除浏览器站点数据、重置浏览器配置或更换浏览器可能导致这些数据无法访问。

建议定期使用顶栏“备份”按钮导出完整备份：单个 JSON 文件包含全部课程、章节、笔记、截图、附件、复习卡片和学习计时。导入备份时可以先预览其中的课程和章节数量，然后选择“替换当前全部数据”或“合并导入新课程”（仅添加备份中当前不存在的课程，按课程 id 去重）。

建议定期使用“导出 Markdown”备份重要课程。导出的单个 `.md` 文件包含：

- 当前课程名称
- 使用纯 Markdown 层级列表的章节目录
- 每章的笔记内容
- 每章的视频截图、视频时间点和截图说明

视频截图使用标准 Markdown 图片语法引用应用托管副本的 `file:///C:/...` 本机绝对路径，不包含 Base64 或 HTML。该方式只在当前电脑且托管图片仍位于原路径时有效；移动到其他电脑、移动项目目录或删除 `data/managed` 后图片会失效。章节回顾、学习状态、计时、标签、复习卡片和章节附件不会加入导出文件。粗体统一导出为 `**文字**`，斜体为 `*文字*`，重点为 `==文字==`，代码块使用带语言名称的围栏格式。

导出前程序会自动探测本地文件夹桥接：通过 `npm run dev` 启动时，截图会先同步到托管目录并使用 `file:///` 绝对路径；如果在生产静态环境（`npm run preview` 或直接打开 `dist/index.html`）或同步失败，程序会自动把最新批注截图内嵌为 Base64 并提示内嵌数量，保证导出的 Markdown 自包含、图片不丢失。

`data/managed` 是运行时产生的本地托管副本，已被 Git 忽略，不会提交到远程仓库。Markdown 只保存指向其中截图文件的本地路径，不会把图片二进制写进 Markdown。

## 12. 构建生产版本

在 `frontend` 目录运行：

```powershell
npm run build
```

构建结果位于 `frontend/dist`。可以用下面的命令本地预览：

```powershell
npm run preview
```

注意：单独部署 `dist` 静态文件不包含 Windows 文件夹桥接服务。需要“一键打开文件夹”功能时，请使用 Electron 桌面版或 `npm run dev`。运行 `npm run desktop:pack` / `npm run desktop:build` 前请先关闭正在运行的 StudyMKD 桌面程序，避免 Windows 占用输出文件。

## 13. 可选：编译 C++ 后端

在项目根目录执行，替换下面的 vcpkg 路径：

```powershell
cd backend
cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE=C:/path/to/vcpkg/scripts/buildsystems/vcpkg.cmake
cmake --build build --config Release
.\build\Release\keji_backend.exe
```

后端默认只监听本机 `127.0.0.1:8787`。接口说明见 [docs/API.md](./docs/API.md)。当前 Web 版仍使用浏览器 IndexedDB 和前端 OCR，启动 C++ 后端不是运行前端的必要步骤。

## 14. 常见问题

### 页面打不开

确认 `npm run dev` 仍在运行，并使用终端显示的实际端口。如果 5173 被占用，Vite 可能选择其他端口。

### 目录图片没有识别结果

使用文字清晰、正向拍摄、对比度较高的截图。首次识别需要加载本地模型，速度可能稍慢。识别后请在导入窗口中校对章节层级。

### 点击打开文件夹没有反应

确认系统是 Windows，并且页面由 `npm run dev` 启动。该功能通过只监听本机的开发服务调用 Windows 资源管理器，直接打开 `dist/index.html` 无法使用。

### 导出 Markdown 时提示截图已内嵌

生产静态构建（`npm run preview` 或直接打开 `dist/index.html`）没有本地文件夹桥接，无法把截图保存到 `data/managed` 并生成 `file:///` 路径，因此程序会自动把最新批注截图内嵌到 Markdown 中。这样导出文件在任何地方都能正常显示图片，但体积会变大；需要引用本机托管路径的导出时，请改用 `npm run dev` 启动。

### 笔记是否会消耗网络流量

日常编辑、OCR、IndexedDB 保存和打开本地文件夹都在本机完成，不需要上传到云端。安装依赖时 npm 需要联网，推送 GitHub 或访问外部资源时也会使用网络。

### 如何更新依赖后检查项目

```powershell
cd frontend
npm run lint
npm run build
```
